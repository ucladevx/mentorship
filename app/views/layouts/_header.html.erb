<!-- Header -->
<header>
      <!-- Navbar -->

        <div class="ui page grid">
        <div class="computer tablet only row">
            <div class="ui borderless fixed menu navbar navbar-main">
                <a href="/" class="logo-wrapper-link">
                    <%= image_tag("Pencil.png", class: "logo mobile-hidden" )%>
                    <%= image_tag("Logo.png", class: "logo") %> </a>


                <div class="right menu">
                    <%= link_to mentors_index_path, class:"item" do %>
                        <span>Mentors</span>
                    <% end %>
                    <%= link_to notes_path, class:"item" do %>
                        <span>Notes</span>
                    <% end %>
                    <%= link_to tutorials_index_path, class:"item" do %>
                        <span>Tutorials</span>
                    <% end %>
                     <% if user_signed_in? %> 
                            <a class="item profile-trigger-modal">
                                <span id="profile-btn"></span>
                            </a>

                            <div class="modal-container hide profile-modal-container">
                                <div class="profile profile-modal">
                                    <div class="profile-arrow-up"></div>
                                    <span id="profile-icon"></span>
                                    <div class="modal-header">
                                        <span class="item">Name</span>
                                    </div>
                                    <div class="modal-field-content">
                                        <%= link_to "My Account", mentors_index_path, class:"item"%>
                                        <%= link_to "Help", notes_path, class:"item"%>
                                        <% if user_signed_in? %> 
                                            <%= link_to destroy_user_session_path, method: :delete, class:"item" do %>
                                                <span class="login btn-main">LOG out</span>
                                        <% end %>
                                        
                                    </div>
                                </div>
                                <div onClick="hideModal('profile')"></div>
                            </div>
                            
                    <% end %>
                    
                    <% else %>
                        <a class="item login-trigger-modal" >
                            <span class="login btn-main">LOG in</span>
                        </a>
                    <% end %>
                </div>
            </div>

        </div>
        <div class="mobile only narrow row">
            <div class="ui fixed menu navbar">
                <div class="ui middle aligned image logo-wrapper-link">
                  <a href="/"><%= image_tag("Pencil.png") %> </a>
                </div>
                <div class="right menu open">
                    <a href="" class="menu item">
                        <i id="chevron" class="navbar-chevron icon chevron down"></i>
                    </a>
                </div>
            </div>
            <div class="ui borderless vertical fixed navbar menu shadow">
                <div class="menu">
                    <%= link_to "Mentors", mentors_index_path, class:"item"%>
                    <%= link_to "Notes", notes_path, class:"item"%>
                    <%= link_to "Tutorials", tutorials_index_path, class:"item"%>
                    <% if user_signed_in? %> 
                         <%= link_to destroy_user_session_path, method: :delete, class:"item" do %>
                            <span class="login btn-main">Logout</span>
                         <% end %>
                    
                    <% else %>
                        <a class="item" id="login-trigger-modal" >
                            <span>Login</span>
                        </a>
                    <% end %>
                </div>
            </div>
        </div>

</header>
