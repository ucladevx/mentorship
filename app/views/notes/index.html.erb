<html>
  <head>
    <title>Mentorship</title>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag ('homepage.js') %>
    <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">

  </head>
  <body>

  

  </body>
</html>

<div class="notes-content">
  <% if !flash[:notice].blank? %>
     <div class = "alert alert-info">
        <%= flash[:notice] %>
     </div>
  <% end %>

<!--
  <%= link_to "New Note", new_note_path, class: "btn btn-primary" %>


  <% @notes.each do |note| %>
   <div>
      <p>Title: <%= note.title %></p>
      <p><%= link_to "Download Note", note.ufile.url %></p>
      <%= button_to "Delete",  note, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{note.title}?" %>
      <%= link_to "^", upvote_path, remote: true %>
      <div>
        <%= note.reputation %>
        <%= link_to "v", downvote_path, remote: true %>i
      </div>
   </div>
  <% end %>
-->
</div>